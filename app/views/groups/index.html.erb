<nav class="nav-bar d-flex justify-content-between align-items-center">
  <span id="back-button" class="material-symbols-outlined ms-2">menu</span>
  <span>CATEGORIES</span>
  <span id="next-button" class="material-symbols-outlined me-2"><%= link_to "logout", destroy_user_session_path, data: { "turbo-method": :delete }, class: 'text-white' %></span>
</nav>

<main id="groups" class="mt-5">
  <% @groups.each do |group| %>
    <%= render group %>
    
  <% end %>
</main>

<%= link_to "New group", new_group_path %>
